<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Overlay de esquinas 16dp para imágenes (ShapeableImageView) -->
    <style name="ShapeAppearance.App.RoundedMedium"
        parent="ShapeAppearance.MaterialComponents.MediumComponent">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">16dp</item>
    </style>

    <!-- Overlay de esquinas 12dp para items del NavigationView -->
    <style name="ShapeAppearance.App.RoundedSmall"
        parent="ShapeAppearance.MaterialComponents.SmallComponent">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">12dp</item>
    </style>

    <!-- Estilo del NavigationView -->
    <style name="Widget.App.NavigationView" parent="Widget.MaterialComponents.NavigationView">
        <item name="itemIconTint">@color/drawer_item_color</item>
        <item name="itemTextColor">@color/drawer_item_color</item>
        <item name="itemBackground">@drawable/drawer_item_bg</item>

        <!-- Tamaño/padding de íconos -->
        <item name="itemIconSize">24dp</item>
        <item name="itemIconPadding">8dp</item>

        <!-- Bordes redondeados para cada item -->
        <item name="itemShapeAppearanceOverlay">@style/ShapeAppearance.App.RoundedSmall</item>
    </style>

</resources>
